[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-codex"
version = "0.0.1"
description = "A learning-first Coding Agent Harness"
requires-python = ">=3.9"
dependencies = [
    "openai",
]

[tool.setuptools.packages.find]
where = ["src"]
